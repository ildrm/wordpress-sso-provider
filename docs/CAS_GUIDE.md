# CAS guide

Status: **Experimental, not exposed.** CAS ticket storage is hashed, expiring,
service-bound, and atomically consumed. Integration tests reject the wrong service
and ticket reuse. Login, validation XML, registered-service administration, SSO,
and logout endpoints are absent; phpCAS interoperability has not run.

