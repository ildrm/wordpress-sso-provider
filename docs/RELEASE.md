# Release record

## 0.1.0 — pre-release foundation

Build identifier: generated from the release ZIP SHA-256 at build time.

This release establishes the validated IAM/domain/storage/WordPress foundation.
It is not an externally usable SSO provider yet. The exact support and test state
is recorded in `docs/PROTOCOL_SUPPORT.md` and `docs/TESTING.md`.

Release artifacts exclude tests, Docker configuration, source-control metadata,
development dependencies, caches, raw frontend sources, and test credentials.

