# OpenID Connect guide

Status: **Experimental, not exposed.** A secure discovery metadata model is unit
tested, but no discovery URL, authorization flow, JWKS, UserInfo, or logout
endpoint is registered. This prevents relying parties from discovering a partial
OP. OIDC conformance and independent RP testing have not run.

