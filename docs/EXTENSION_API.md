# Extension API guide

Status: **Not public.** Internal ports currently cover application persistence;
the broader protocol, identity-source, authentication-method, policy-condition,
template, and audit-listener registration API is not stable or exported. Third
parties must not depend on internal namespaces in 0.1.0.

