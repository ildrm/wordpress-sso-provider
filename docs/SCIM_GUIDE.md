# SCIM guide

Status: **Experimental, not exposed.** The framework-independent parser accepts a
small allowlisted RFC 7644 filter subset and rejects compound/injection input.
Users, Groups, discovery, authentication, PATCH, pagination, and ETags are not
registered. No provisioning client should target this release.

