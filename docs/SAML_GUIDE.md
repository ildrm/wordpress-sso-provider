# SAML guide

Status: **Unsupported.** The release lockfile includes OneLogin's SAML toolkit and
xmlseclibs so future work does not hand-write XML signature primitives. No IdP
adapter, metadata endpoint, AuthnRequest parser, assertion issuer, SLO endpoint,
or certificate UI is present. No SAML compatibility claim is made.

