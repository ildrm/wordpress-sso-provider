# WP-CLI guide

Status: **Not implemented.** The Docker verification harness uses ordinary WP-CLI
core/plugin/eval commands, but no `wp sso` command namespace is registered by the
plugin. This document intentionally does not advertise planned commands as real.

